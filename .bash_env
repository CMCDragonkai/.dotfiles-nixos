# ~/.bash_env
# Bash environment hook used by ~/.bashrc (and optionally BASH_ENV if you ever choose).
# Keep it *environment-only* (no output, no aliases, no prompt).

# Load the shared session environment
if [ -r "$HOME/.profile" ]; then
  source "$HOME/.profile"
fi