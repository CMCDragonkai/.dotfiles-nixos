# ~/.xprofile
# SDDM (and most display managers) source this for X11 sessions.
# Keep it *environment-only* (no interactive shell config).
# It is not read from `xinit`. So starting X from a text mode will not
# load .xprofile unless it is configured inside .xinitrc.

# Guard against double sourcing
[ -n "$__XPROFILE_SOURCED" ] && return
export __XPROFILE_SOURCED=1

if [ -r "$HOME/.profile" ]; then
  source "$HOME/.profile"
fi